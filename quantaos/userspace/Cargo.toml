[workspace]
resolver = "2"
members = [
    "libquanta",
    "init",
    "shell",
    "coreutils",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Zain Dana Harper"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
